TODO
# Lello et al BIORXIV deep learning
# Fix indicators for range, weekdays, sometimes


# GIT flow
git add <file(s)>
# Commit and push steps for every change
git commit -m "Message"
git push origin master

# Useful commands
# Remove file locally and from repository
git rm --cached <file>
git rm --cached -r <directory>

# Add version
git tag 1.0.0 <commit id>

# Check log (also find commit id
git log
